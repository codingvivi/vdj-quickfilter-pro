# VirtualDJ Quickfilter-Pro

This VirtualDJ plugin adds quickfilters
with more complex logic and behaviors, like:

- treating certain #tags as numerical values,
  filtering for ranges of those values
- Batch updating on master track change
